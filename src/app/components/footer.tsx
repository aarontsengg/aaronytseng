const Footer = () => (
    <footer className="w-full py-6 text-center text-gray-600">
        <div className="mt-8 pt-6 border-t border-border/10 text-center">
            <p className="text-xs text-muted-foreground">© {new Date().getFullYear()} aaron tseng</p>
        </div>
    </footer>
);

export default Footer;

